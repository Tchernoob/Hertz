{% extends 'base.html.twig' %}

{% block title %}Sketch{% endblock %}

{% block body %}
    <h1>Sketch</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sketch.id }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ sketch.year }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ sketch.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sketch_index') }}">back to list</a>

    <a href="{{ path('app_sketch_edit', {'id': sketch.id}) }}">edit</a>

    {{ include('sketch/_delete_form.html.twig') }}
{% endblock %}
